# Reference thumbnails (optional)

Drop example YouTube thumbnails here (`.jpg` / `.png`).

The app uses dominant colors from the first image when the AI background API fails.

For best results, also share references in chat when tuning the design.
